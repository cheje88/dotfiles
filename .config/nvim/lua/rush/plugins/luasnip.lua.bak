return {
  "L3MON4D3/LuaSnip",
  dependencies = { "rafamadriz/friendly-snippets" },
  -- follow latest release.
  version = "v2.*", -- Replace <CurrentMajor> by the latest released major (first number of latest release)
  -- install jsregexp (optional!).
  build = "make install_jsregexp",

  -- require("luasnip.loaders.from_vscode").lazy_load(),
  -- require("luasnip.loaders.from_vscode").lazy_load({ paths = { "/home/rush/.config/nvim/snippets/" } }), -- Load snippets from my-snippets folder -- require("luasnip.loaders.from_vscode").lazy_load(), -- You can pass { paths = "./my-snippets/"} as well
  -- will exclude all javascript snippets
  -- require("luasnip.loaders.from_vscode").load({
  --   exclude = { "javascript" },
  -- }),
}
